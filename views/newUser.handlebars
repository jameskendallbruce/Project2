<div style="margin: 150px;">

    <a href="/">Return Home</a>

    <form>
        {{!-- action="POST" --}}
        <h1>New User Sign Up</h1>
        <h3>Username</h3>
        <input type="text" id="username">
        <h3>Password</h3>
        <input type="password" id="password">
        <h3>First Name</h3>
        <input type="text" placeholder="John" id="first_name">
        <br>
        <h3>Last Name</h3>
        <input type="text" placeholder="Doe" id="last_name">
        <br>
        <h3>Email Address</h3>
        <input type="text" placeholder="emailaddress@google.net" id="email">
        <br>
        <h3>StreetAddress</h3>
        <input type="text" placeholder="Street Adress" id="address">
        <br>
        <h3>City</h3>
        <input type="text" placeholder="City" id="city">
        <br>
        <h3>State</h3>
        <input type="text" placeholder="State" id="state">
        <br>
        <h3>Zip</h3>
        <input type="text" placeholder="Zip" id="zip">
        <br>
        <button type="submit" id="createNewUser">Create New User</button>
            <div id="warningAllFieldsRequiredUser">
                <p>* All fields are mandatory</p>
            </div>
    </form>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

<script>

    function redirectUser(){
      window.location.href="/userOptions";
    }

    function captureNewUser(event) {
        event.preventDefault()

        console.log("success")
                //{{!-- var food_name = $("#food_name").val().trim();
        //var food_type = $("#food_type").val().trim();
        //var description = $("#food_description").val().trim();
        //var price = parseFloat($("#price").val().trim());
        //var delivery = $("#delivery").val().trim(); --}}

        var username = $("#username").val().trim();
        var password =  $("#password").val().trim();
        var first_name = $("#first_name").val().trim();
        var last_name = $("#last_name").val().trim();
        var email = $("#email").val().trim();
        var address = $("#address").val().trim();
        var city = $("#city").val().trim();
        var state = $("#state").val().trim();
        var zip = $("#zip").val().trim();

        if (username == "" ||
            password ==  "" ||
            first_name == "" ||
            last_name == "" ||
            email == "" ||
            address == "" ||
            city == "" ||
            state == "" ||
            zip == "") {
            $("#warningAllFieldsRequiredUser").show()
        } else {
            var newUser = {
                username: username,
                password: password,
                first_name: first_name,
                last_name: last_name,
                email: email,
                address: address,
                city: city,
                state: state,
                zip: zip
            }
                
            $.post('/newUser', newUser).then(console.log).catch(console.error);

            redirectUser();
        }

        console.log(newUser);


    };

    $("form").on("submit", captureNewUser);

</script>